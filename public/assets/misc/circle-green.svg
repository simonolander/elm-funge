<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
    <!--
        >>> r = 0.6
        >>> " ".join([ "L" + str(c) + "," + str(d) for (c, d) in [[[(cos(-tau/4 + i * tau/5) * 50 + 50, sin(-tau/4 + i * tau/5) * 50 + 50), (cos(-tau/4 + tau / 10 + i * tau/5) * 50 * r + 50, sin(-tau/4 + tau/10 + i * tau/5) * 50 * r + 50)] for i in range(5)][a][b] for a in range(5) for b in range(2)]])[1:]
    -->
    <circle fill="#008000" cx="50" cy="50" r="50"/>
</svg>
